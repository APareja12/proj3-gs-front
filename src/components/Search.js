const Search = () => { 
    
const buttonEl = doument.getElementById('search-btn')   
const inputEl = doument.getElementById('search-input')   
    
buttonEl.addEventListener('click', handleClick);
function handleClick() {
    console.log('Clicked');
}
    return (
       <>
      
       </>
    );
}

export default Search;