import { StyledMain } from '../styles';

const Favorites = (props) => {
    return (
        <StyledMain>
            <h1>Coming Soon...</h1>
        </StyledMain>
    );
};

export default Favorites;